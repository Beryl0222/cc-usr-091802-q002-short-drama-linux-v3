# 海外短剧制片协作

本项目服务网文改编短剧的跨市场制作。领域资料分别记录来源作品、地区授权、剧本版本、国际制片团队、拍摄素材和发行版本，避免不同参与方用错权利范围或内容版本。

`fixtures/sample.json` 给出横琴拍摄基地和协作团队的示例规模。授权、脚本与媒体资产采用独立标识，任何派生关系都应能回到来源，仓库不包含真实剧本、演员证件或未公开素材。

基础入口可用 `python3 service.py --check` 检查，`python3 -m unittest -v` 会验证服务标识和样例数据。启动服务后可通过 `/health` 查看运行状态。
